STRICT INSTRUCTIONS — FOLLOW EXACTLY

==================================================
0. DO NOT BREAK EXISTING SYSTEM
===============================

* DO NOT change Clerk authentication
* DO NOT modify existing routes or core logic
* DO NOT redesign entire UI structure
* ONLY enhance UI styling + fix bugs + add features

==================================================

1. FIX "INVALID ID" BUG (CRITICAL)
   ==================================================

ISSUE:
Clicking auction → shows "invalid id"

FIX:

Step 1: Ensure navigation sends ID
navigate(`/buyer/auction/${auction.id}`)

Step 2: In auction page:
import { useParams } from "react-router-dom";
const { id } = useParams();

Step 3: Match correctly:
const auction = auctions.find(a => String(a.id) === id);

Step 4: Add fallback:
if (!auction) return <div>Auction not found</div>;

IMPORTANT:

* Ensure auction list is loaded before searching
* ID must be compared as string

==================================================
2. REAL-TIME BIDDING SYSTEM (IMPORTANT)
=======================================

REQUIREMENT:

* All users must see SAME bid updates instantly

IMPLEMENT:

* Use shared state (or backend if exists)
* When one user places bid:
  → update global auction state
  → reflect to all users

Seller must see:

* Live updating bids
* List of clients bidding

Buyer must see:

* Updated highest bid in real time

==================================================
3. GLOBAL AUCTION TIMER (SYNCED)
================================

* Each auction has ONE endTime
* All users see SAME countdown

Timer logic:
const timeLeft = endTime - Date.now();

When time ends:

* Disable bidding
* Show "Auction Ended"

==================================================
4. SELLER LIVE CLIENT VIEW
==========================

Seller Clients Page must show:

* Client ID
* Address
* Bid amount

Table:

* Live updating
* Sorted by highest bid

Buttons:

* Accept
* Reject

==================================================
5. PAYMENT SYSTEM (SIMULATED)
=============================

Create Payment Page:

Requirements:

* Only ONLINE payments:

  * UPI
  * PhonePe
  * Card

NO cash on delivery

Flow:
Winner → Payment Page → Select method → Confirm

==================================================
6. AGENT SYSTEM (IMPORTANT)
===========================

Add system role: "Agent"

Logic:

* After payment:
  → money goes to "agent" (NOT seller directly)

* Status:

  * "Held by Agent"
  * "Released after delivery"

Add field:
paymentStatus:

* pending
* held_by_agent
* completed

==================================================
7. UI ENHANCEMENT (DO NOT CHANGE STRUCTURE)
===========================================

IMPORTANT:

* DO NOT redesign layout
* ONLY enhance visuals

REMOVE:

* pure black background

ADD:

* reddish gradient overlay

Example:
background: linear-gradient(135deg, #1a0a0a, #3b0f19, #6a1b2e);

==================================================
8. ANIMATED BACKGROUND (SUBTLE)
===============================

Add:

* floating blurred circles
* slow movement animation

Effect:

* soft glow
* low opacity
* smooth motion

==================================================
9. CARD & BUTTON ENHANCEMENT
============================

Cards:

* glass effect (transparent + blur)
* soft red glow border

Buttons:

* red/pink gradient
* hover glow
* smooth scale animation

==================================================
10. NAVBAR ENHANCEMENT
======================

* Add slight transparency
* Add glow hover effect
* Keep structure SAME

==================================================
11. FINAL UX REQUIREMENTS
=========================

* No page should break
* No navigation issues
* Smooth transitions
* Real-time feel

==================================================
FINAL RESULT
============

* Clicking auction → works (no invalid ID)
* Real-time bidding visible
* Seller sees live client bids
* Payment system works (simulated)
* Agent holds payment
* UI upgraded (reddish animated theme)
* Existing system remains intact
